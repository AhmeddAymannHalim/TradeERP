@model PaginatedResult<CustomerViewModel>
@{
    ViewBag.Title = L["CustomerList"];
}

<div class="row">
    <div class="col-12">
        <div class="d-flex gap-2 mb-3 flex-wrap justify-content-between align-items-center">
            <form asp-action="Index" method="get" class="d-flex align-items-center gap-2">
                <input type="search" class="form-control" name="searchString" value="@Model.SearchString" placeholder="@L["SearchHere"]" style="max-width: 250px;" />
                <button type="submit" class="btn btn-outline-secondary">
                    <i class="bi bi-search"></i>
                </button>
            </form>
            <a asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-lg"></i> @L["New"]
            </a>
        </div>

        <div class="card table-responsive">
            <table class="table mb-0 text-nowrap table-hover">
                <thead class="table-light border-light">
                    <tr>
                        <th>#</th>
                        <th>@L["Code"]</th>
                        <th>@L["ArabicName"]</th>
                        <th>@L["EnglishName"]</th>
                        <th>@L["PhoneNumber"]</th>
                        <th>@L["Actions"]</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Data.Any())
                    {
                        var counter = (Model.PageNo - 1) * Model.PageSize + 1;
                        foreach (var item in Model.Data)
                        {
                            <tr class="align-middle">
                                <td>@counter</td>
                                <td>@item.Code</td>
                                <td>@item.ArName</td>
                                <td>@item.EnName</td>
                                <td>@item.Phone</td>
                                <td>
                                    <a asp-action="Update" asp-route-id="@item.Id"><i class="bi bi-pencil"></i></a>
                                    <a href="javascript:void(0)" onclick="Delete('/Customer/Delete/@item.Id')" class="link-danger ms-2"><i class="bi bi-trash"></i></a>
                                </td>
                            </tr>
                            counter++;
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="6" class="text-center">@L["NoData"]</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @await Html.PartialAsync("_Pagination", Model)
    </div>
</div>
