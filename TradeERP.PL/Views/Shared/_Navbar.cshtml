<nav class="navbar-custom">
    <button class="button-menu-mobile" id="sidebarToggle" type="button">
        <i class="bi bi-list"></i>
    </button>

    <span class="page-title-main">@ViewData["Title"]</span>

    <ul class="topnav-menu-right">
        <partial name="_SelectLanguagePartial" />

        <li class="dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                <i class="bi bi-person-circle"></i>
                <span class="ms-1">@(User.Identity?.Name ?? L["Admin"].Value)</span>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item" asp-controller="Account" asp-action="Settings"><i class="bi bi-gear me-2"></i>@L["Settings"]</a></li>
                <li><hr class="dropdown-divider"></li>
                <li>
                    <form asp-controller="Account" asp-action="Logout" method="post" class="px-3">
                        <button type="submit" class="dropdown-item p-0 border-0 bg-transparent">
                            <i class="bi bi-box-arrow-right me-2"></i>@L["SignOut"]
                        </button>
                    </form>
                </li>
            </ul>
        </li>
    </ul>
</nav>
